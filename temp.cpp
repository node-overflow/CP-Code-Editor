#include "bits.h"

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

vector<int> v;
for (int i = 0; i < 5; i++) v.push_back(i);
for (int i = 0; i < 5; i++) cout << v[i] << " ";
cout << endl;
    int n;
    cin >> n;
    cout << n << "\n";

    return 0;
}